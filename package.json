{
  "name": "ignis",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Ludus Ignis — a roguelite creature-collector with tamagotchi care, set in the Ludus Ignis world. See DESIGN.md.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "vite": "^5.4.0",
    "vite-plugin-solid": "^2.11.0",
    "vitest": "^2.1.0"
  }
}
